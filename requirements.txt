# Stillpoint — Python Dependencies
# Install: pip install -r requirements.txt

# Core
gradio>=6.20  # 6.14 frontend drops visibility updates on hidden components
chromadb>=1.5.0
mempalace>=3.3.5
pyyaml>=6.0
jinja2>=3.1


# NotebookLM CLI — NOT a pip dependency. It is an external CLI installed via
# pipx (it would otherwise pull Playwright into this venv). scripts/setup.sh
# installs it automatically:  pipx install "notebooklm-py[browser]"

# LLM Providers (install the ones you need)
anthropic>=0.18
openai>=1.0
google-generativeai

# Utilities
python-dateutil>=2.8

# Optional: Local podcast generation (v2)
# podcastfy